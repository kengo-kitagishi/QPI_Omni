# QPI 解析フロー — 現状整理（2026-03-03）

> このドキュメントは会話で聞き取った「今の実際の解析フロー」の記録。
> コードの整理・変更は目的ではなく、今後の会話での参照用。

---

## 研究の目的

**グルコース飢餓下の酵母細胞（S. pombe）の体積・乾燥質量の時系列トラッキング**

培地を途中で切り替えながら（例: 2% → 0% → 2%）、細胞の物理量の変化をQPI（定量位相イメージング）で追う。

---

## データの種類と命名規則

### `wo_*` シリーズ（空チャネル背景画像）

- **wo_0**: 0% グルコース培地の空（細胞なし）チャネル
- **wo_2**: 2% グルコース培地の空チャネル
- **wo_0.0055**, **wo_0.01**, **wo_0.04** 等: 各中間濃度
- **内容**: z方向 0.4µm 刻みの画像（タイムラプスではない。~41枚）
- **用途1**: 本番前の焦点位置確認
- **用途2**: 本番タイムラプスの背景引き算の基準（培地ごとに異なるRIに対応）

### `ph_1`（本番タイムラプスデータ）

- 細胞入りのタイムラプス実験
- 現状はフォルダ名に実験条件が入っていない（本来は条件をフォルダ名に含めるべき）
- 各 Pos フォルダ内に時系列画像が入っている
- 各フレームには**複数のマイクロ流体チャネルが写っている**
- **フレーム間隔**: 5分
- **欠損フレーム**: 基本的になし

---

## 解析フロー（実験タイムライン順）

### フェーズ0: 実験準備（撮影前）

1. マイクロ流体デバイスに細胞を導入
2. 各培地濃度の**空チャネル画像（wo_*）を撮影**
   - z方向 0.4µm 刻み × 複数 Pos 位置
3. 細胞導入後に**焦点確認用 z-stack を撮影**
   - 同じく 0.4µm 刻み

### フェーズ1: 焦点位置の確認

**スクリプト**: `21_align_and_subtract.py`

- `wo_*` の同 z 位置画像を基準として、細胞入り z-stack に引き算
- 差分画像を目視で確認し、どの z 位置が焦点に合っているか判断
- **記録すること**: 最良の z 位置（インデックス）
- **方針**: 焦点 z は**全 Pos 共通固定**

### フェーズ2: 本番タイムラプス撮影（ph_1）

- 設定した焦点位置で細胞タイムラプスを開始
- 培地を切り替えながら撮影:
  - 例: 2% (24h) → 0% (48h) → 2%
  - 切り替えタイミングを記録しておく

### フェーズ3: 位相再構成

**スクリプト**: `scripts/10_batch_reconstruction_new.py`

- 入力: 外付けHDD上の生ホログラム画像（`img_*_Default_*.tif`）
- 出力: `output_phase/*.tif`（float32位相画像）
- 要確認パラメータ:
  - `OFFAXIS_CENTER`: 毎回FFTで確認が必要
  - `CROP`: 画像のクロップ領域
  - 光学定数は `scripts/optical_config.py` で管理

### フェーズ4: 手動 Crop（ImageJ）

- 各フレームには複数のマイクロ流体チャネルが写っている
- **ImageJで手動**で1チャネル領域を選択してCrop
- 1チャネル分のタイムラプスデータを Pos ごとに多数作成
- ※ 現状はスクリプト化されていない（煩雑なステップ）

### フェーズ5: ガウス背景補正

**スクリプト**: `scripts/19_gaussian_backsub.py` （`19_gausian_backsub.py` も同じ）

- 問題: Pos0 と PosN の波面ずれで各フレームに位相傾斜が乗る
- 対処: ヒストグラムのピーク（培地部分）をガウスフィットして0にシフト
  - ピーク検出閾値: `-1.1 rad` 以下（チャネル以外の部分）
  - チャネルが写っていようと、培地のみの領域でピークを検出
- 出力: `*_bg_corr.tif`
- **最適化指標**: 背景 ROI 平均 ≈ 0
- **Top Hat 比較**: 評価軸は「画像の端と端の明るさ差がなくなるか」「細胞への影響」

### フェーズ6: wo_ 引き算（細胞のみ情報を抽出）

**スクリプト**: `scripts/36_align_and_subtract_timelapse.py`（主力）
**確認用**: `scripts/34_align_and_subtract_simple.py` + `scripts/shift_visualize.py`

- 各 Pos の背景補正済み画像から、対応する `wo_*` 画像をアライメントして引く
- アライメントシフト量を確認: **1px 以下が許容範囲**
  - シフトが大きいほどノイズが乗るため
- **培地切り替え時刻**: 画像番号（何枚目から）を正とする。ユーザーが指定する
- **wo_* の前提**: 同一光学条件で撮影。混合過渡フレーム（流路置換中）はなし

#### 培地切り替え（wo_2 → wo_0 → wo_2）

| 時間帯 | 培地 | 引くべき wo_ |
|--------|------|-------------|
| 0〜24h | 2% glucose | wo_2 |
| 24〜72h | 0% glucose | wo_0 |
| 72h〜 | 2% glucose | wo_2 |

- 36 スクリプトの `MEDIA_SWITCHES` でフレーム番号（＝画像番号、ファイル名末尾）を指定して切り替え可能

#### ⚠️ 未決定

- **シフト > 1px のフレーム**: 再アライメント / 除外 / 警告のいずれにするか未定

### フェーズ7: セグメンテーション

**事前準備**:
1. Omnipose GUI でアノテーション（~3000 細胞の ground truth マスク作成）
2. データ拡張: `12_vertical_flip.py`, `26_horizontal_flip.py`
3. モデル学習: `08_train.py`（normalize=False, rescale=False が重要）

**推論**:
- スクリプト: `07_segmentation.py`
- 出力: `*_masks.tif`, `*_binary.tif`

- **学習データ**: 0% 条件と 2% 条件の両方を十分含める

### フェーズ8: 輪郭線抽出

**スクリプト**: `00_contours.py`（おそらく）

- Omnipose マスクから細胞輪郭を抽出

### フェーズ9: 細胞トラッキング（ImageJ）

- **ImageJ カスタムマクロ**で1細胞ずつトラッキング
- 出力: 各フレームの Major axis, Minor axis, Slice 番号
- **分裂時 ID 継承**: Mother Machine のため、チャネル奥の細胞を追い続ける
- **OmniSegger 移行**: 可視化機能を重視。自作 Omnipose モデルを使用。IoU 等の整合判定は不要

### フェーズ10: 体積・RI 計算

**スクリプト**: `scripts/32_simple_ellipse_ri.py`

- 体積: Rod shape（カプセル型 = 円柱 + 半球×2）近似
  ```
  V = (4/3)πr³ + πr²h  (r = Minor/2, h = Major - 2r)
  ```
- RI: マスク内の位相値の合計 / 理論体積
- **出力方針**: absolute RI を正とする（流す培地の RI が違うため ΔRI より信用できる）
- **n_medium**: 実測値を使いたい（未測定）。0% と 2% は別値にする予定。温度は一定のため温度補正不要

#### ⚠️ 未決定

- **n_medium 実測**: 測定手順・タイミングの検討
- **specific refractive increment (α)**: 文献未調査。条件依存にするか単一固定か未定

---

## 成果物・実験方針

- **最終成果物の正本**: 図
- **1 実験あたりの Pos 数**: 取れるだけ
- **Pass/Fail 閾値**: 未定義（まとめたい）

---

## 現在の進捗状況

| ステップ | 状態 |
|---------|------|
| 位相再構成 | ✅ 動いている |
| 手動 Crop | ⚠️ 手動（ボトルネック） |
| ガウス背景補正 | ⚠️ 動くが最適化中 |
| wo_ 引き算 | ⚠️ 36 の MEDIA_SWITCHES で培地切り替え対応済み |
| セグメンテーション | ⚠️ 一度は通しているが未確立 |
| トラッキング | ⚠️ ImageJ マクロで対応中 |
| RI/体積計算 | ❌ 培地 RI 問題が未解決 |

---

## スクリプト対応表（現行）

| 目的 | 現在使っているスクリプト | 旧スクリプト |
|------|--------------------------|-------------|
| 位相再構成 | `10_batch_reconstruction_new.py` | `10_batch_reconstruction.py` |
| 焦点確認 | `21_align_and_subtract.py` | - |
| ガウス背景補正 | `19_gaussian_backsub.py` | - |
| タイムラプス引き算 | `36_align_and_subtract_timelapse.py` | `21_calc_alignment.py` + `22_ecc_alignment.py` + `04_diff_from_first.py` |
| シフト確認 | `34_align_and_subtract_simple.py` + `shift_visualize.py` | - |
| セグメンテーション学習 | `08_train.py` | - |
| セグメンテーション推論 | `07_segmentation.py` | - |
| 輪郭抽出 | `00_contours.py` | - |
| RI/体積計算 | `32_simple_ellipse_ri.py` | `24_ellipse_volume.py` |

---

## 課題・TODO（コード整理ではなく方法論の検討事項）

1. **ガウス背景補正 vs Top Hat 変換** の比較検討（評価軸: 端の明るさ差、細胞への影響）
2. **n_medium 実測**の測定手順確立
3. **α 文献調査**（条件依存の要否）
4. **36_align シフト > 1px** 時の扱い（再アライメント / 除外 / 警告）の決定
5. **Pass/Fail 閾値**の定義（シフト、背景平均、mask 面積など）
6. **手動 Crop ステップ**のスクリプト化（将来的に）
