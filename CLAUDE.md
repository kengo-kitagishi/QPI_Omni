# QPI_Omni

ルール本体は **`AGENTS.md`** にある（dotfiles の `CLAUDE.md` から生成してコミットしている）。
Claude Code は `CLAUDE.md` と `AGENTS.md` の両方を読むので、ここには重複して書かない。

- ルールを直すとき: dotfiles の `CLAUDE.md` を直す → `AGENTS.md` を生成し直してコミットする
- claude.ai / スマホ用の短い版: `docs/CLAUDE_PREFERENCES.md`

以前この `CLAUDE.md` は `C:\Users\QPI\dotfiles\claude\CLAUDE.md` へのシンボリックリンクだったが、
Web・スマホのセッションや別OSのPCからは読めなかったため実ファイルにした。
dotfiles 側でこのリンクを張り直さないこと。
