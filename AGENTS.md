# AGENTS.md

このリポジトリのルールは `CLAUDE.md` にある。そちらを読むこと。

全体のルール（Notion GTD の運用・固定スケジュール・文章の書き方）は
`~/.claude/CLAUDE.md`（dotfiles の `claude/CLAUDE.md`）にある。

同じ内容をこのファイルに書き写さない。2026-09-10 まで、このファイルには
半年前の写しが残っていて「予定・タスク管理はすべて ClickUp」と書いてあった。
Claude は `CLAUDE.md` を、Codex は `AGENTS.md` を読むので、二重に持つと必ず片方が古くなる。
